<p id="notice"><%= notice %></p>

<h3><strong><%= link_to "Back", home_index_path %></strong></h3>

<h1>Specialities</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Charge</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @specialities.each do |speciality| %>
      <tr>
        <td><%= speciality.name %></td>
        <td><%= speciality.charge %></td>
        <td><%= link_to 'Show', speciality %></td>
        <td><%= link_to 'Edit', edit_speciality_path(speciality) %></td>
        <td><%= link_to 'Destroy', speciality, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h2><%= link_to 'New Speciality', new_speciality_path %></h2>
